from enum import Enum


class Expression_type(Enum):
    GT = 0 # greater than
    GEQ = 1    # greater than or equal to
    LT = 2    # less than
    LEQ = 3    # less than or equal to
    EQ = 4    # equal to
